<!DOCTYPE html>
<html>
<head>
    <title>Detalle del Producto</title>
</head>
<body>
    <h1>{{ product.name }}</h1>
    <p><strong>Precio:</strong> {{ product.price }}</p>
    <p><strong>Descripción:</strong> {{ product.description }}</p>
    <a href="{{ path('show') }}">Volver a la lista de productos</a>
</body>
</html>
